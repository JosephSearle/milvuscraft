{
  "mcpServers": {
    "milvus": {
      "command": "uvx",
      "args": [
        "mcp-proxy",
        "--transport",
        "streamablehttp",
        "${user_config.milvus_mcp_url}"
      ]
    }
  }
}
